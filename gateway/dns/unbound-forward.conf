# Unbound — forwarders só depois que o túnel sobe.
# Substitua PLACEHOLDER_TUNNEL_DNS pelo resolvedor no peer próprio.
# Não aponte para o DNS do ISP quando o kill switch estiver armado.

forward-zone:
  name: "."
  forward-addr: PLACEHOLDER_TUNNEL_DNS
